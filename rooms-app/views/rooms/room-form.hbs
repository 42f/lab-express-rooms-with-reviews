<h1>{{appTitle}} 🚀</h1>
<h2>{{formTitle}}</h2>

<form action="{{formAction}}" method="post">
	{{> formTextField value="name" name="Name" required=true }}
	{{> formTextField value="description" name="Description" required=true }}
	{{> formTextField value="imageUrl" name="Url to a photo" }}
	<input type='submit' placeholder="0" value='{{formButton}}' />
</form>


{{#if error}}
	<p>{{error}}</p>
{{/if}}

