<label for="{{value}}">{{name}}</label>
<input
	{{#if required}} required {{/if}}
	{{#if definedValue}} value={{definedValue}} {{/if}}
	id="{{value}}" name="{{value}}"
	{{#if type}} type="{{type}}" {{else}}type="text"  {{/if}}
	>
